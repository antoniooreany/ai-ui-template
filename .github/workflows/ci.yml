name: Continuous Integration

on:
  push:
    branches: [ develop, main ]
  pull_request:
    branches: [ develop, main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: ai-ui-template/package-lock.json

    - name: Install dependencies
      run: npm ci
      working-directory: ai-ui-template

    - name: Run tests
      run: npm test
      working-directory: ai-ui-template
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
